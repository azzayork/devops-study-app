[project]
name = "kubernetes"
version = "0.1.0"
description = "Add your description here"
authors = [{ name = "Aaron York", email = "azza.york@icloud.com" }]
requires-python = ">=3.14"
dependencies = [
    "requests>=2.34.2",
]

[project.scripts]
kubernetes = "kubernetes:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
